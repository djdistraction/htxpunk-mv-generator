# UX — jobs

- Every long step is a Job: queued | running | succeeded | failed | cancelled  
- UI polls jobs; shows progress % and message  
- HTTP never blocks the event loop on Demucs/Whisper/Remotion  
- Failed step: Retry on that step only (vocals ≠ lyrics)  
- Activity panel always visible  
